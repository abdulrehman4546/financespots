name: Deploy to mHosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Trigger Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Ping hosting webhook
        run: |
          echo "Push to main detected — hosting webhook will auto-deploy"
          echo "Commit: ${{ github.sha }}"
          echo "By: ${{ github.actor }}"
